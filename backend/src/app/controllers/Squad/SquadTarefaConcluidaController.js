import db from '../../../database/connection';

class SquadTarefaConcluidaController{
   
    async update(req, res){
       
        return res.json({});
    }

   
}

export default new SquadTarefaConcluidaController();